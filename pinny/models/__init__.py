"""Inference-time learned models (docs/phase2-contracts.md P5, P6).

Import the submodules directly (``pinny.models.verifier``,
``pinny.models.artifact``). torch is imported lazily inside them, so
importing this package never requires the ``train`` extra.
"""
